// import functions and grab DOM elements
const potatoHPEl = document.getElementById('');
// let state

// set event listeners 
  // get user input
  // use user input to update state 
  // update DOM to reflect the new state

const testData = {
    id: 1,
    name: 'Papa Cabeza',
    hp: 10
};

potatoHPEl.textContent = testData.hp;

